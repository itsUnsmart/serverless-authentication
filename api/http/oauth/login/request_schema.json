{
    "definitions": {},
    "type": "object",
    "title": "AccountLogin",
    "required": ["code", "clientid", "provider"],
    "properties": {
        "code": {
            "type": "string",
            "title": "Code from the provider",
            "minLength": 1
        },
        "clientid": {
            "type": "string",
            "title": "The id of the client authorizing the user",
            "minLength": 1
        },
        "provider": {
            "type": "string",
            "title": "Name of the provider used to login",
            "pattern": "^[a-zA-Z]+$",
            "minLength": 1
        }
    }
}
